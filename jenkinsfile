pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                dir('C:/Users/User/Documents/GitHub/Actividad_jenkins') {
                    sh 'npm install'  // Instala las dependencias si es necesario
                    sh 'node app.js &'  // Inicia tu aplicación Node.js en segundo plano
                }
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
            }
        }
    }
    post {
        always {
            echo '========always========'
        }
        failure {
            echo '========pipeline execution failed========'
        }
    }
}
